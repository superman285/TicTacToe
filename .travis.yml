language: node_js
node_js: "11"
addons:
  chrome: stable
sudo: required
script:
  - npm test
  - npm run build
after_script:
  - git init
  - git config --global user.name 'superman285'
  - git config --global user.name 'superman285@126.com'
  - git add .
  - git commit -m "rebuild TicTacToe project,update the dist directory"
  - git push --force "https://${GITHUB_TOKEN}@${GITHUB_REPO}" master:master

env:
  global:
    - GITHUB_REPO: github.com/superman285/TicTacToe.git